<a type="{{ type|default('button') }}"
   href="{{ link|default('#') }}"
   class="mx-2 px-6 py-2 transition ease-in duration-200 uppercase rounded-full hover:bg-black hover:text-white border-2 border-gray-900 focus:outline-none cursor-pointer">
    <span class="font-semibold">{{ label }}</span>
</a>